<!-- ROM identity card shown in Software information. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/unica_rom_identity_background"
        android:fontFamily="sans-serif-medium"
        android:paddingStart="22dp"
        android:paddingEnd="22dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:text="@string/unica_rom_identity_title"
        android:textColor="#000000"
        android:textSize="20sp" />

    <ImageView
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginStart="10dp"
        android:contentDescription="@string/unica_rom_identity_sparkle_content_description"
        android:src="@drawable/unica_rom_identity_sparkle" />

</LinearLayout>
